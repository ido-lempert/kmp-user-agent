{
  "name": "kmp-user-agent",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "webApp",
    "library/build/dist/js/developmentLibrary"
  ],
  "scripts": {
    "build:shared": "./gradlew :library:jsBrowserDevelopmentLibraryDistribution",
    "build": "npm run build:shared && npm run build -w webApp",
    "start": "npm run build:shared && npm run start -w webApp"
  }
}